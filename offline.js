fetch('/data/current.json').then(r=>{if(!r.ok)throw Error();return r.json();}).then(data=>{document.getElementById('asof').textContent='Snapshot '+data.asOf+' · Stored for offline reading';const root=document.getElementById('chart');data.artists.forEach(a=>{const row=document.createElement('p');row.className='row';row.textContent=`${a.rank}. ${a.name} · ${a.score}`;root.append(row);});}).catch(()=>document.getElementById('asof').textContent='Open this page while online once to save the chart.');
