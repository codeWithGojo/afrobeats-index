const {Resvg}=require('@resvg/resvg-js');const path=require('path');const {data,escape:e}=require('../lib/shared.cjs');
module.exports=(req,res)=>{const a=data.artists.find(a=>a.slug===req.query.artist);if(!a)return res.status(404).end();const story=req.query.format==='story',w=story?1080:1200,h=story?1920:630;const movement=Number.isFinite(a.previousRank)?a.previousRank-a.rank:null;const nameSize=Math.min(story?90:76,900/Math.max(a.name.length*.6,1));const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="${w}" height="${h}"><rect width="100%" height="100%" fill="#1C1C1A"/><g font-family="DejaVu Sans"><text x="72" y="105" font-size="36" fill="#D7F04A">AFR / INDEX</text><text x="72" y="${story?480:310}" font-size="${story?280:180}" font-weight="bold" fill="#D7F04A">#${a.rank}</text><text x="72" y="${story?650:420}" font-size="${nameSize}" fill="#E8E6D8">${e(a.name)}</text><text x="72" y="${story?760:485}" font-size="30" fill="#E8E6D8">CURRENT · ${movement===null?'Movement unavailable':movement>0?`UP ${movement}`:movement<0?`DOWN ${-movement}`:'NO CHANGE'}</text><path d="M72 ${h-200}H${w-72}" stroke="#4A4940"/><text x="72" y="${h-140}" font-size="24" fill="#aaa89c">Snapshot ${e((data.asOf||'').slice(0,10))} · Editorial ranking</text><text x="72" y="${h-85}" font-size="22" fill="#D7F04A">afrobeats-index.vercel.app/current/${e(a.slug)}</text></g></svg>`;try{const png=new Resvg(svg,{font:{fontFiles:[path.join(process.cwd(),'assets/DejaVuSans.ttf')],loadSystemFonts:false}}).render().asPng();res.setHeader('Content-Type','image/png');res.setHeader('Cache-Control','public,max-age=0,s-maxage=3600');res.end(png);}catch{res.status(500).end();}};
