(function(){function start(){const original=window.showTab;if(original)window.showTab=function(id){original(id);const url=new URL(location.href);url.searchParams.set('tab',id);history.replaceState({},'',url);};if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});const nav=document.querySelector('.nav-scroll');if(nav){const a=document.createElement('a');a.href='/studio';a.textContent='STUDIO ↗';a.className='nav-btn';nav.append(a);}const params=new URLSearchParams(location.search);if(params.get('tab')&&document.getElementById(params.get('tab')))window.showTab?.(params.get('tab'));document.addEventListener('keydown',e=>{if(e.key==='/'&&!/INPUT|TEXTAREA|SELECT/.test(e.target.tagName)&&!e.target.isContentEditable){e.preventDefault();location.href='/studio';}});const current=document.getElementById('current');if(current){const a=document.createElement('a');a.href='/studio';a.textContent='Movers, ranking cards & discovery ↗';current.prepend(a);}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();})();
